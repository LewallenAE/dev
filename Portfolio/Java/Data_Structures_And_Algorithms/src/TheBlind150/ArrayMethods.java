package TheBlind150;

import java.util.*;
import java.util.ArrayList;

public class ArrayMethods {


    public static void main(String[] args) {
        //int arr[]  = new int[] {10, 39, 49, 580, 2, 39};
        //int [] nums = {100, 50, 25, 75, 5, 200, 1};

        //List<Integer> list = new ArrayList<>();
        //list = Arrays.asList(10, 20 , 30, 50 , 90, 100, 22);
        // sort
        //Arrays.sort(list);


        //Arrays.sort(arr, 1, arr.length -1 );
        //Arrays.sort(nums, 2, nums.length -1 );

        // sort a range
        //System.out.println(Arrays.toString(nums));

        List<Integer> list = new ArrayList<>();
        // list.add(10);  instead of this we can use:

        list = Arrays.asList(10,29,540,3,4,5,67,89,10);
    }
}
